# ===============================
# DATABASE (Supabase Postgres)
# ===============================
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# Important: Don't let Hibernate auto-modify schema on production
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ===============================
# MAILJET EMAIL CONFIG
# ===============================
mailjet.api.key=${MAILJET_API_KEY}
mailjet.api.secret=${MAILJET_API_SECRET}
mail.sender.email=${MAIL_SENDER_EMAIL}
mail.sender.name=${MAIL_SENDER_NAME}

# Spring Mail SMTP Configuration (Mailjet)
spring.mail.host=${SPRING_MAIL_HOST}
spring.mail.port=${SPRING_MAIL_PORT}
spring.mail.username=${SPRING_MAIL_USERNAME}
spring.mail.password=${SPRING_MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# ===============================
# JWT
# ===============================
jwt.secret=${JWT_SECRET}

# ===============================
# SUPABASE STORAGE
# ===============================
SUPABASE_URL=${SUPABASE_URL}
SUPABASE_SERVICE_KEY=${SUPABASE_SERVICE_KEY}
SUPABASE_BUCKET_PRODUCT_IMAGES=product-images
SUPABASE_BUCKET_REVIEW_IMAGES=review-images
SUPABASE_BUCKET_CATEGORY_IMAGES=category-images

# ===============================
# CORS
# ===============================
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS}

# ===============================
# SERVER PORT (Render / Cloud)
# ===============================
# Render sets the port in $PORT environment variable
server.port=${PORT:8080}

# ===============================
# OPTIONAL: Debug (remove for production)
# ===============================

