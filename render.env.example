# Render environment variables - example (DO NOT COMMIT SECRETS)
# Copy these values into Render Dashboard (Dashboard → <service> → Environment → Environment Variables)
# IMPORTANT: Do NOT surround values with extra quotes. If you paste values, make sure there are no leading/trailing quotes or newlines.

# Preferred: set the full JDBC URL (no quotes) OR set the individual pieces below
# Option A: Full JDBC URL (preferred):
#SPRING_DATASOURCE_URL=jdbc:postgresql://aws-1-eu-west-1.pooler.supabase.com:6543/postgres?user=postgres.widewqtjdgbphbksxpco&password=YOUR_DB_PASSWORD&sslmode=require&prepareThreshold=0

# Option B: Individual pieces (avoid comma/quote problems when dashboard formatting is involved)
SPRING_DATASOURCE_HOST=aws-1-eu-west-1.pooler.supabase.com
SPRING_DATASOURCE_PORT=6543
SPRING_DATASOURCE_USERNAME=postgres.widewqtjdgbphbksxpco
SPRING_DATASOURCE_PASSWORD=YOUR_DB_PASSWORD
SPRING_DATASOURCE_DBNAME=postgres
SPRING_DATASOURCE_PARAMS=?sslmode=require&prepareThreshold=0

# Alternatively set the full URL but ensure Render does not add quotes or commas around it
# SPRING_DATASOURCE_URL=jdbc:postgresql://${SPRING_DATASOURCE_HOST}:${SPRING_DATASOURCE_PORT}/${SPRING_DATASOURCE_DBNAME}?user=${SPRING_DATASOURCE_USERNAME}&password=${SPRING_DATASOURCE_PASSWORD}${SPRING_DATASOURCE_PARAMS}

# Server
PORT=5000

# Mailjet
MAILJET_API_KEY=YOUR_MAILJET_API_KEY
MAILJET_API_SECRET=YOUR_MAILJET_API_SECRET
MAILJET_API_URL=https://api.mailjet.com/v3.1/send
MAILJET_ENABLED=true
MAIL_SENDER_EMAIL=hloniyacho@gmail.com
MAIL_SENDER_NAME=MAISON LUXE

# JWT
JWT_SECRET=REPLACE_ME_WITH_SECURE_RANDOM

# Supabase (if used)
SUPABASE_URL=https://widewqtjdgbphbksxpco.supabase.co
SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY
SUPABASE_SERVICE_KEY=YOUR_SUPABASE_SERVICE_KEY

# CORS
APP_CORS_ALLOWED_ORIGINS=https://client-hub-portal.vercel.app,http://localhost:5173,http://localhost:3000

# Notes:
# - In Render Dashboard, prefer using the individual host/username/password variables if the UI auto-quotes values with commas.
# - When copying the full JDBC URL, remove any surrounding quotes and ensure no trailing newline characters exist.
# - After setting values in Render, trigger a redeploy (or use a manual deploy) so the environment variables are used at build/runtime.
