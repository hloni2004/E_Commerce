# Render Blueprint Configuration
# Note: Set environment variables in Render Dashboard for security
# Dashboard > Environment > Environment Variables

services:
  - type: web
    name: maison-luxe-backend
    runtime: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/E-Commerce-1.0-SNAPSHOT.jar
    plan: free
